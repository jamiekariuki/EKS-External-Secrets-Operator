===== FILE: ./charts/app/Chart.yaml =====

apiVersion: v2
name: app
description: A Helm chart for Kubernetes
type: application
version: 0.1.0
appVersion: "1.16.0"

#dependencies:
#- name: external-secrets
#  version: 1.0.0
#  repository: https://charts.external-secrets.io
#  alias: eso


===== FILE: ./charts/app/.helmignore =====

# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/


===== FILE: ./charts/app/templates/app/backend/deployment.yaml =====

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.backend.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- range $k, $v := .Values.backend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      {{- range $k, $v := .Values.backend.labels }}
      {{ $k }}: {{ $v }}
      {{- end }}
  template:
    metadata:
      labels:
        {{- range $k, $v := .Values.backend.labels }}
        {{ $k }}: {{ $v }}
        {{- end }}
    spec:
      serviceAccountName: {{ .Values.backend.serviceAccountName | default "default" }}
      containers:
        - name: {{ .Values.backend.name }}
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.backend.containerPort }}
          resources:
            requests:
              cpu: {{ .Values.backend.resources.requests.cpu }}
              memory: {{ .Values.backend.resources.requests.memory }}
            limits:
              cpu: {{ .Values.backend.resources.limits.cpu }}
              memory: {{ .Values.backend.resources.limits.memory }} 
          envFrom:
            - secretRef:
                name: {{ .Values.backend.envFrom.secretRef.name }} 
            - configMapRef:
                name: {{ .Values.dbConfig.name }}
             




===== FILE: ./charts/app/templates/app/backend/hpa.yaml =====



===== FILE: ./charts/app/templates/app/backend/service.yaml =====

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.backend.service.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- range $k, $v := .Values.backend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
spec:
  type: {{ .Values.backend.service.type | default "ClusterIP" }}
  selector:
    {{- range $k, $v := .Values.backend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port | default 8000 }}
      targetPort: {{ .Values.backend.containerPort }}


===== FILE: ./charts/app/templates/app/frontend/deployment.yaml =====

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.frontend.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- range $key, $value := .Values.frontend.labels }}
    {{ $key }}: {{ $value }}
    {{- end }}
spec:
  replicas: {{ .Values.frontend.replicas }}
  selector:
    matchLabels:
      {{- range $key, $value := .Values.frontend.labels }}
      {{ $key }}: {{ $value }}
      {{- end }}
  template:
    metadata:
      labels:
        {{- range $key, $value := .Values.frontend.labels }}
        {{ $key }}: {{ $value }}
        {{- end }}
    spec:
      serviceAccountName: {{ .Values.frontend.serviceAccountName | default "default" }}
      containers:
        - name: {{ .Values.frontend.name }}
          image: "{{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}"
          imagePullPolicy: {{ .Values.frontend.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.frontend.containerPort }}
          resources:
            requests:
              cpu: {{ .Values.frontend.resources.requests.cpu }}
              memory: {{ .Values.frontend.resources.requests.memory }}
            limits:
              cpu: {{ .Values.frontend.resources.limits.cpu }}
              memory: {{ .Values.frontend.resources.limits.memory }}
       


===== FILE: ./charts/app/templates/app/frontend/hpa.yaml =====



===== FILE: ./charts/app/templates/app/frontend/service.yaml =====

apiVersion: v1
kind: Service
metadata:
  name: {{.Values.frontend.service.name}}
  namespace: {{ .Values.namespace }}
  labels:
    {{- range $k, $v := .Values.frontend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
spec:
  type: {{ .Values.frontend.service.type | default "NodePort" }}
  selector:
    {{- range $k, $v := .Values.frontend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
  ports:
    - name: http
      port: {{ .Values.frontend.service.port | default 80 }}
      targetPort: {{ .Values.frontend.containerPort }}
      {{- if .Values.frontend.service.nodePort }}
      nodePort: {{ .Values.frontend.service.nodePort }}
      {{- end }}



===== FILE: ./charts/app/templates/config-map.yaml =====

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.dbConfig.name }}
  namespace: {{ .Values.namespace }}
data:
  POSTGRES_DB: {{ .Values.dbConfig.dbname }}
  POSTGRES_HOST: {{ .Values.dbConfig.host }}
  POSTGRES_PORT: {{ .Values.dbConfig.port | quote }}



===== FILE: ./charts/app/templates/external-secret.yaml =====

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecret.name }}
  namespace: {{ .Values.namespace }}
spec:
  refreshInterval: {{ .values.externalSecret.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.externalSecret.secretStore.name }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.externalSecret.target }}
    creationPolicy: Owner
  data:
  - secretKey: POSTGRES_USER
    remoteRef:
      conversionStrategy: Default	
      decodingStrategy: None
      metadataPolicy: None
      key: {{ .Values.externalSecret.remoteRef.key }}
      property: username
  - secretKey: POSTGRES_PASSWORD
    remoteRef:
      conversionStrategy: Default	
      decodingStrategy: None
      metadataPolicy: None
      key: {{ .Values.externalSecret.remoteRef.key }}
      property: password


===== FILE: ./charts/app/templates/httproute.yaml =====

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.app.HTTPRoute.name }}
  namespace: {{ .Values.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.app.HTTPRoute.syncWaves | quote }} 
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ .Values.gateway.name }}          
      namespace: {{ .Values.gateway.namespace }} 
  hostnames:
    - {{ .Values.app.HTTPRoute.hostName }}           
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.app.HTTPRoute.path }}      
      backendRefs:
        - group: ''	
          kind: Service
          name: {{ .Values.app.HTTPRoute.serviceName }} 
          port: {{ .Values.app.HTTPRoute.port }}
          weight: 1

===== FILE: ./charts/app/values-dev.yaml =====

namespace: dev
frontend:
  image:
    tag: v0.1.35
backend:
  image:
    tag: v0.1.24
config:
  host: dev-db.c0nu6occexai.us-east-1.rds.amazonaws.com
app:
  HTTPRoute:
    name: dev-app-route
    hostName: "dev.example.com"
externalSecret:
  remoteRef:
    key: arn:aws:secretsmanager:us-east-1:466872246229:secret:rds!db-af91fbb1-35d5-4750-89ea-724be13ddc24-rJNfy5


===== FILE: ./charts/app/values-prod.yaml =====

namespace: prod
  
frontend:
  image:
    tag: v0.1.30
backend:
  image:
    tag: v0.1.16

config:
  host: empty

app:
  HTTPRoute:
    name: prod-app-route
    hostName: "prod.example.com" 
externalSecret:
  remoteRef:
    key: arn:aws:secretsmanager:us-east-1:466872246229:secret:rds!db-01cbfbc8-9e69-4556-8968-5684a2ffb86e-8jD5rK


===== FILE: ./charts/app/values-stage.yaml =====

namespace: stage
frontend:
  image:
    tag: v0.1.35
backend:
  image:
    tag: v0.1.24
config:
  host: empty
app:
  HTTPRoute:
    name: stage-app-route
    hostName: "stage.example.com"
externalSecret:
  remoteRef:
    key: arn:aws:secretsmanager:us-east-1:466872246229:secret:rds!db-01cbfbc8-9e69-4556-8968-5684a2ffb86e-8jD5rK


===== FILE: ./charts/app/values.yaml =====

namespace: default
#===========================================
#                 app                      
#===========================================

#http route
gateway:
  name: eg
  namespace: gateway
app:
  HTTPRoute:
    name: dev-app-route
    syncWaves: "1"
    hostName: "dev.example.com"
    path: /
    serviceName: frontend
    port: 80
#frontend
frontend:
  name: frontend
  replicas: 1
  labels:
    app: frontend
    component: frontend
  image:
    repository: 466872246229.dkr.ecr.us-east-1.amazonaws.com/frontend-repository
    tag: "latest"
    pullPolicy: IfNotPresent
  containerPort: 80
  serviceAccountName: default
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "300m"
      memory: "256Mi"
  podAnnotations: {}
  env: []
  #service
  service:
    name: frontend
    type: ClusterIP
    port: 80
#backend
backend:
  name: backend
  replicas: 1
  labels:
    app: backend
    component: backend
  image:
    repository: 466872246229.dkr.ecr.us-east-1.amazonaws.com/backend-repository
    tag: "latest"
    pullPolicy: IfNotPresent
  containerPort: 8000
  serviceAccountName: default
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "400m"
      memory: "512Mi"
  podAnnotations: {}
  envFrom:
    secretRef:
      name: db-credentials #must match with external secret store target 
  #service    
  service:
    name: backend
    type: ClusterIP
    port: 8000
#config    
dbConfig:
  name: db-credentials
  dbname: posgressdb
  host: "dev-db.c0nu6occexai.us-east-1.rds.amazonaws.com"
  port: "5432"
#external secrete
externalSecret:
  secretStore:
    name: cluster-secret-store
  name: db-credentials
  target: db-credentials
  refreshInterval: 5m0s
  remoteRef:
    key: arn:aws:secretsmanager:us-east-1:466872246229:secret:rds!db-01cbfbc8-9e69-4556-8968-5684a2ffb86e-8jD5rK


===== FILE: ./charts/argocd/Chart.yaml =====

apiVersion: v2
name: argocd
description: A Helm chart for Kubernetes

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"


===== FILE: ./charts/argocd/.helmignore =====

# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/


===== FILE: ./charts/argocd/templates/httproute.yaml =====

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.argocd.HTTPRoute.name }}
  namespace: {{ .Values.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.argocd.HTTPRoute.syncWaves | quote }} 
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ .Values.gateway.name }}          
      namespace: {{ .Values.gateway.namespace }} 
  hostnames:
    - {{ .Values.argocd.HTTPRoute.hostName }}           
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.argocd.HTTPRoute.path }}      
      backendRefs:
        - group: ''	
          kind: Service
          name: {{ .Values.argocd.HTTPRoute.serviceName }} 
          port: {{ .Values.argocd.HTTPRoute.port }}
          weight: 1



===== FILE: ./charts/argocd/values.yaml =====

gateway:
  name: eg
  namespace: gateway

namespace: argocd

argocd:
  HTTPRoute:
    name: argocd-route
    syncWaves: "1"
    hostName: "argocd.example.com"
    path: /
    serviceName: argocd-server
    port: 80




===== FILE: ./charts/backend/Chart.yaml =====

apiVersion: v2
name: backend
description: A Helm chart for Kubernetes

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"


===== FILE: ./charts/backend/.helmignore =====

# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/


===== FILE: ./charts/backend/templates/deployment.yaml =====

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.backend.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- range $k, $v := .Values.backend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      {{- range $k, $v := .Values.backend.labels }}
      {{ $k }}: {{ $v }}
      {{- end }}
  template:
    metadata:
      labels:
        {{- range $k, $v := .Values.backend.labels }}
        {{ $k }}: {{ $v }}
        {{- end }}
    spec:
      serviceAccountName: {{ .Values.backend.serviceAccountName | default "default" }}
      containers:
        - name: {{ .Values.backend.name }}
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.backend.containerPort }}
          resources:
            requests:
              cpu: {{ .Values.backend.resources.requests.cpu }}
              memory: {{ .Values.backend.resources.requests.memory }}
            limits:
              cpu: {{ .Values.backend.resources.limits.cpu }}
              memory: {{ .Values.backend.resources.limits.memory }} 
          envFrom:
            - secretRef:
                name: {{ .Values.backend.envFrom.secretRef.name }} 
           
             




===== FILE: ./charts/backend/templates/hpa.yaml =====



===== FILE: ./charts/backend/templates/service.yaml =====

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.backend.service.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- range $k, $v := .Values.backend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
spec:
  type: {{ .Values.backend.service.type | default "ClusterIP" }}
  selector:
    {{- range $k, $v := .Values.backend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port | default 8000 }}
      targetPort: {{ .Values.backend.containerPort }}


===== FILE: ./charts/backend/values.yaml =====

namespace: dev
#backend
backend:
  name: backend
  replicas: 1
  labels:
    app: backend
    component: backend
  image:
    repository: 290488660393.dkr.ecr.us-east-1.amazonaws.com/eso-backend
    tag: "latest"
    pullPolicy: IfNotPresent
  containerPort: 8080
  serviceAccountName: default
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "400m"
      memory: "512Mi"
  podAnnotations: {}
  envFrom:
    secretRef:
      name: db-credentials #must match with external secret store target 
  #service    
  service:
    name: backend
    type: ClusterIP
    port: 8080
#external secrete
externalSecret:
  secretStore:
    name: cluster-secret-store
  name: db-credentials
  target: db-credentials
  refreshInterval: 5m0s
  remoteRef:
    key: eso-dev/rds/credentials


===== FILE: ./charts/eso/Chart.yaml =====

apiVersion: v2
name: eso
description: A Helm chart for Kubernetes

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"


===== FILE: ./charts/eso/.helmignore =====

# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/


===== FILE: ./charts/eso/templates/external-secret.yaml =====

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecret.name }}
  namespace: {{ .Values.namespace }}
spec:
  refreshInterval: {{ .Values.externalSecret.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.externalSecret.secretStore.name }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.externalSecret.target }}
    creationPolicy: Owner
  data:
  - secretKey: DB_USER
    remoteRef: 
      conversionStrategy: Default	
      decodingStrategy: None
      metadataPolicy: None
      key: {{ .Values.externalSecret.remoteRef.key }}
      property: username
  - secretKey: DB_PASSWORD 
    remoteRef:
      conversionStrategy: Default	
      decodingStrategy: None
      metadataPolicy: None
      key: {{ .Values.externalSecret.remoteRef.key }}
      property: password
  - secretKey: DB_HOST
    remoteRef:
      conversionStrategy: Default	
      decodingStrategy: None
      metadataPolicy: None
      key: {{ .Values.externalSecret.remoteRef.key }}
      property: host 
  - secretKey: DB_PORT 
    remoteRef:
      conversionStrategy: Default	
      decodingStrategy: None
      metadataPolicy: None
      key: {{ .Values.externalSecret.remoteRef.key }}
      property: port
  - secretKey: DB_NAME
    remoteRef:
      conversionStrategy: Default	
      decodingStrategy: None
      metadataPolicy: None
      key: {{ .Values.externalSecret.remoteRef.key }}
      property: dbname          


===== FILE: ./charts/eso/templates/secret-store.yaml =====

apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: {{ .Values.secretStore.name }}
spec:
  provider:
    aws:
      service: {{ .Values.secretStore.provider.service }}
      region: {{ .Values.secretStore.provider.region }}
      auth:
        jwt:
          serviceAccountRef:
            name: {{ .Values.secretStore.serviceAccount.name }}
            namespace: {{ .Values.secretStore.serviceAccount.namespace }}
            


===== FILE: ./charts/eso/values.yaml =====

namespace: dev
#secrete store
secretStore:
  name: cluster-secret-store
  provider:
    service: SecretsManager
    region: us-east-1
  serviceAccount:
    name: external-secrets-sa
    namespace: external-secrets
#external secrete
externalSecret:
  secretStore:
    name: cluster-secret-store
  name: db-credentials
  target: db-credentials
  refreshInterval: 5m0s
  remoteRef:
    key: eso-dev/rds/credentials



===== FILE: ./charts/frontend/Chart.yaml =====

apiVersion: v2
name: frontend
description: A Helm chart for Kubernetes

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"


===== FILE: ./charts/frontend/.helmignore =====

# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/


===== FILE: ./charts/frontend/templates/deployment.yaml =====

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.frontend.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- range $key, $value := .Values.frontend.labels }}
    {{ $key }}: {{ $value }}
    {{- end }}
spec:
  replicas: {{ .Values.frontend.replicas }}
  selector:
    matchLabels:
      {{- range $key, $value := .Values.frontend.labels }}
      {{ $key }}: {{ $value }}
      {{- end }}
  template:
    metadata:
      labels:
        {{- range $key, $value := .Values.frontend.labels }}
        {{ $key }}: {{ $value }}
        {{- end }}
    spec:
      serviceAccountName: {{ .Values.frontend.serviceAccountName | default "default" }}
      containers:
        - name: {{ .Values.frontend.name }}
          image: "{{ .Values.frontend.image.repository }}:{{ .Values.frontend.image.tag }}"
          imagePullPolicy: {{ .Values.frontend.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.frontend.containerPort }}
          resources:
            requests:
              cpu: {{ .Values.frontend.resources.requests.cpu }}
              memory: {{ .Values.frontend.resources.requests.memory }}
            limits:
              cpu: {{ .Values.frontend.resources.limits.cpu }}
              memory: {{ .Values.frontend.resources.limits.memory }}
       


===== FILE: ./charts/frontend/templates/hpa.yaml =====



===== FILE: ./charts/frontend/templates/service.yaml =====

apiVersion: v1
kind: Service
metadata:
  name: {{.Values.frontend.service.name}}
  namespace: {{ .Values.namespace }}
  labels:
    {{- range $k, $v := .Values.frontend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
spec:
  type: {{ .Values.frontend.service.type | default "NodePort" }}
  selector:
    {{- range $k, $v := .Values.frontend.labels }}
    {{ $k }}: {{ $v }}
    {{- end }}
  ports:
    - name: http
      port: {{ .Values.frontend.service.port | default 80 }}
      targetPort: {{ .Values.frontend.containerPort }}
      {{- if .Values.frontend.service.nodePort }}
      nodePort: {{ .Values.frontend.service.nodePort }}
      {{- end }}



===== FILE: ./charts/frontend/values.yaml =====

namespace: dev
#frontend
frontend:
  name: frontend
  replicas: 1
  labels:
    app: frontend
    component: frontend
  image:
    repository: 290488660393.dkr.ecr.us-east-1.amazonaws.com/eso-frontend
    tag: "latest"
    pullPolicy: IfNotPresent
  containerPort: 80
  serviceAccountName: default
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "300m"
      memory: "256Mi"
  podAnnotations: {}
  env: []
  #service
  service:
    name: frontend
    type: ClusterIP
    port: 80


===== FILE: ./charts/gateway/Chart.yaml =====

apiVersion: v2
name: gateway
description: A Helm chart for Kubernetes

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"



===== FILE: ./charts/gateway/.helmignore =====

# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/


===== FILE: ./charts/gateway/templates/gateway-class.yaml =====

apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: {{ .Values.gatewayClass.name }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.gatewayClass.syncWaves | quote }}
spec:
  controllerName: {{ .Values.gatewayClass.controllerName }}

===== FILE: ./charts/gateway/templates/gateway.yaml =====

apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: {{ .Values.gateway.name }}
  namespace: {{ .Values.gateway.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.gateway.syncWaves | quote }}
spec:
  gatewayClassName: {{ .Values.gatewayClass.name }}
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All

===== FILE: ./charts/gateway/templates/httproute.yaml =====

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.app.HTTPRoute.name }}
  namespace: {{ .Values.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.app.HTTPRoute.syncWaves | quote }} 
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ .Values.gateway.name }}          
      namespace: {{ .Values.gateway.namespace }} 
  hostnames:
    - {{ .Values.app.HTTPRoute.hostName }}           
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: {{ .Values.app.HTTPRoute.path }}      
      backendRefs:
        - group: ''	
          kind: Service
          name: {{ .Values.app.HTTPRoute.serviceName }} 
          port: {{ .Values.app.HTTPRoute.port }}
          weight: 1

===== FILE: ./charts/gateway/values.yaml =====

namespace: dev

gatewayClass:
  name: eg
  controllerName: gateway.envoyproxy.io/gatewayclass-controller
  syncWaves: "0"

gateway:
  name: eg
  namespace: gateway
  syncWaves: "1"


app:
  HTTPRoute:
    name: dev-app-route
    syncWaves: "2"
    hostName: "dev.example.com"
    path: /
    serviceName: frontend
    port: 80  



===== FILE: ./.github/scripts/extract-tf-outputs.sh =====

#!/bin/bash
# extract-tf-outputs.sh
# Fetches Terraform state from S3 and exports all outputs to GITHUB_ENV.
#
# Usage:
#   extract-tf-outputs.sh <bucket> <environment> <state-key>
#
# Args:
#   bucket       — S3 bucket name (TF_STATE_BUCKET)
#   environment  — workspace name, e.g. dev, stage, prod
#   state-key    — path within the bucket, e.g. infra/terraform.tfstate
#
# Exports to GITHUB_ENV:
#   IRSA_ARN, SECRETS_MANAGER_ARN, DB_INSTANCE_ADDRESS,
#   DB_INSTANCE_NAME, DB_INSTANCE_PORT, FRONTEND_REPO_URL, BACKEND_REPO_URL

set -euo pipefail

BUCKET="$1"
ENVIRONMENT="$2"
STATE_KEY="$3"

S3_PATH="s3://${BUCKET}/env:/${ENVIRONMENT}/${STATE_KEY}"
TMPSTATE=$(mktemp)
OUTFILE=$(mktemp)

cleanup() { rm -f "$TMPSTATE" "$OUTFILE"; }
trap cleanup EXIT

# ── Fetch state ───────────────────────────────────────────────────────────────
echo "Fetching state: ${S3_PATH}"
aws s3 cp "$S3_PATH" "$TMPSTATE"
 
# ── Extract outputs ───────────────────────────────────────────────────────────
echo "Extracting outputs..."
jq '{
  irsa_arn:                .outputs.irsa_arn.value,
  frontend_repository_url: .outputs.frontend_repository_url.value,
  backend_repository_url:  .outputs.backend_repository_url.value
}' "$TMPSTATE" > "$OUTFILE"

echo "Extracted outputs:"
cat "$OUTFILE"

# ── Export to GITHUB_ENV ──────────────────────────────────────────────────────
echo "Exporting to GITHUB_ENV..."
{
  echo "IRSA_ARN=$(jq -r .irsa_arn "$OUTFILE")"
  echo "FRONTEND_REPO_URL=$(jq -r .frontend_repository_url "$OUTFILE")"
  echo "BACKEND_REPO_URL=$(jq -r .backend_repository_url "$OUTFILE")"
} >> "$GITHUB_ENV"

echo "Done."


===== FILE: ./.github/scripts/update-helm.sh =====

#!/bin/bash
# update-helm.sh
# Updates each Helm chart's values file with Terraform outputs, then commits and pushes.
#
# Charts updated (each independently):
#   frontend  — region, environment, image repository
#   backend   — region, environment, image repository
#   eso       — IRSA ARN for the ESO service account
#
# Usage:
#   update-helm.sh <environment> <region> <irsa_arn> \
#                  <frontend_repo_url> <backend_repo_url> \
#                  <charts_dir>
#
# Args:
#   $1  environment        — dev, stage, prod
#   $2  region             — AWS region
#   $3  irsa_arn           — IRSA role ARN for ESO service account
#   $4  frontend_repo_url  — ECR URL for frontend image
#   $5  backend_repo_url   — ECR URL for backend image
#   $6  charts_dir         — path to charts directory, e.g. ./charts

set -euo pipefail

ENVIRONMENT="$1"
REGION="$2"
IRSA_ARN="$3"
FRONTEND_REPO_URL="$4"
BACKEND_REPO_URL="$5"
CHARTS_DIR="$6"

# ── Validate args ─────────────────────────────────────────────────────────────
for var in ENVIRONMENT REGION IRSA_ARN FRONTEND_REPO_URL BACKEND_REPO_URL CHARTS_DIR; do
  if [ -z "${!var}" ]; then
    echo "ERROR: Missing required argument: ${var}"
    exit 1
  fi
done

# ── Resolve values file paths ─────────────────────────────────────────────────
# Adjust these paths to match your actual chart layout.
FRONTEND_VALUES="${CHARTS_DIR}/frontend/values.yaml"
BACKEND_VALUES="${CHARTS_DIR}/backend/values.yaml"
ESO_VALUES="${CHARTS_DIR}/eso/values.yaml"

for file in "$FRONTEND_VALUES" "$BACKEND_VALUES" "$ESO_VALUES"; do
  if [ ! -f "$file" ]; then
    echo "ERROR: Values file not found: ${file}"
    exit 1
  fi
done

# ── Ensure yq is available ────────────────────────────────────────────────────
if ! command -v yq &> /dev/null; then
  echo "Installing yq..."
  sudo wget -q https://github.com/mikefarah/yq/releases/download/v4.44.1/yq_linux_amd64 \
    -O /usr/bin/yq
  sudo chmod +x /usr/bin/yq
fi

# ── Frontend chart ────────────────────────────────────────────────────────────
echo "Updating frontend values: ${FRONTEND_VALUES}"
yq -i "

  .frontend.image.repository          = \"${FRONTEND_REPO_URL}\"
" "$FRONTEND_VALUES"

# ── Backend chart ─────────────────────────────────────────────────────────────
echo "Updating backend values: ${BACKEND_VALUES}"
yq -i "

  .backend.image.repository          = \"${BACKEND_REPO_URL}\"
" "$BACKEND_VALUES"

# ── ESO chart ─────────────────────────────────────────────────────────────────
#echo "Updating ESO values: ${ESO_VALUES}"
#yq -i "

#  .serviceAccount.roleArn = \"${IRSA_ARN}\"
#" "$ESO_VALUES"

#echo "All chart values updated."

# ── Commit & push ─────────────────────────────────────────────────────────────
# Stage only the charts directory — nothing else in the repo gets touched.

git config --global user.email "ci-bot@github-actions"
git config --global user.name  "CI Bot"

git add "$CHARTS_DIR"
 
if git diff --cached --quiet; then
  echo "No changes to commit — all values already up to date."
  exit 0
fi

git commit -m "chore(${ENVIRONMENT}): sync terraform outputs [skip ci]"

# Discard any remaining unstaged changes before rebasing.
# The runner working tree may have dirty files from earlier pipeline steps
# (ci-secrets.tfvars, plan outputs, etc.) that were never meant to be committed.
# Our helm changes are already committed above so discarding the rest is safe.
echo "Cleaning working tree before sync..."
git restore . 2>/dev/null || true

echo "Syncing with remote before push..."
git fetch origin main
git rebase origin/main

git push origin main
echo "Helm values pushed successfully."

===== FILE: ./.github/workflows/ci.yaml =====

name: Build and Push to ECR

# Manually triggered from the Actions tab — pick frontend or backend from the dropdown.
on:
  workflow_dispatch:
    inputs:
      service:
        description: "Which service to build and push"
        required: true
        type: choice
        options:
          - frontend
          - backend
      image_tag_override:
        description: "Optional: custom image tag (defaults to short git SHA)"
        required: false
        type: string

# Required for GitHub OIDC -> AWS role assumption. Keep this minimal.
permissions:
  id-token: write
  contents: read

# Prevent two deploys of the SAME service from running concurrently.
# (A frontend run and a backend run can still run in parallel.)
concurrency:
  group: deploy-${{ github.event.inputs.service }}
  cancel-in-progress: false

jobs:
  build-and-push:
    name: Build & Push (${{ github.event.inputs.service }})
    runs-on: ubuntu-latest
    # Optional: gate this with a "dev" Environment in repo settings
    # (Settings -> Environments -> production) to require manual approval
    # before the job runs. Remove the line below if you don't want that gate.
    environment: dev
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials via OIDC
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN  }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: gha-${{ github.run_id }}

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Resolve build variables
        id: vars
        env:
          SERVICE: ${{ github.event.inputs.service }}
          IMAGE_TAG_OVERRIDE: ${{ github.event.inputs.image_tag_override }}
          ECR_REPO_FRONTEND: ${{ secrets.ECR_REPOSITORY_FRONTEND }}
          ECR_REPO_BACKEND: ${{ secrets.ECR_REPOSITORY_BACKEND }}
        run: |
          set -euo pipefail

          case "$SERVICE" in
            frontend)
              echo "context_dir=app/frontend" >> "$GITHUB_OUTPUT"
              echo "ecr_repo=${ECR_REPO_FRONTEND}" >> "$GITHUB_OUTPUT"
              ;;
            backend)
              echo "context_dir=app/backend" >> "$GITHUB_OUTPUT"
              echo "ecr_repo=${ECR_REPO_BACKEND}" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "::error::Unknown service '$SERVICE'"
              exit 1
              ;;
          esac

          if [[ -n "${IMAGE_TAG_OVERRIDE}" ]]; then
            TAG="${IMAGE_TAG_OVERRIDE}"
          else
            TAG="${GITHUB_SHA:0:8}"
          fi
          echo "image_tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Sanity check Dockerfile exists
        run: |
          if [[ ! -f "${{ steps.vars.outputs.context_dir }}/Dockerfile" ]]; then
            echo "::error::No Dockerfile found at ${{ steps.vars.outputs.context_dir }}/Dockerfile"
            exit 1
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: ./${{ steps.vars.outputs.context_dir }}
          push: true
          platforms: linux/amd64
          provenance: false
          tags: |
            ${{ steps.vars.outputs.ecr_repo }}:${{ steps.vars.outputs.image_tag }}
          cache-from: type=gha,scope=${{ github.event.inputs.service }}
          cache-to: type=gha,mode=max,scope=${{ github.event.inputs.service }}

      # "latest" is pushed as a separate, best-effort step. If tag immutability
      # is enabled on your ECR repo, this step will fail on repeat runs — that's
      # expected and safe to ignore, or just delete this step if you don't need
      # a "latest" tag at all.
      - name: Also tag and push :latest
        run: |
          docker buildx imagetools create \
            --tag ${{ steps.vars.outputs.ecr_repo }}:latest \
            ${{ steps.vars.outputs.ecr_repo }}:${{ steps.vars.outputs.image_tag }}

      - name: Summary
        run: |
          {
            echo "### Deployment Summary"
            echo "- **Service:** ${{ github.event.inputs.service }}"
            echo "- **ECR repository:** ${{ steps.vars.outputs.ecr_repo }}"
            echo "- **Image tag:** \`${{ steps.vars.outputs.image_tag }}\`"
            echo "- **Image digest:** \`${{ steps.build.outputs.digest }}\`"
            echo "- **Commit:** \`${{ github.sha }}\`"
            echo "- **Triggered by:** ${{ github.actor }}"
          } >> "$GITHUB_STEP_SUMMARY"

===== FILE: ./.github/workflows/infrastructure.yaml =====

# ── Infrastructure Pipeline ────────────────────────────────────────────────────
#
# Full lifecycle management for EKS/VPC infrastructure + ArgoCD app-of-apps.
# This is a MANUAL workflow — triggered via workflow_dispatch only.
#
# ── Job flow (apply) ──────────────────────────────────────────────────────────
#
#   guard → infra-plan → infra-apply → update-helm → argocd-plan → argocd-apply
#                    ↘ (destroy) → destroy
#
# ── Secrets & config split ────────────────────────────────────────────────────
#
#   terraform.<env>.tfvars  (committed) — region, project, cluster_name, etc.
#   ci-secrets.tfvars       (runtime)   — aws_account_id only; never committed
#
# ── GitHub Secrets required ───────────────────────────────────────────────────
#
#   Repository secrets:
#     AWS_OIDC_ROLE_ARN     — OIDC role with Terraform permissions
#     AWS_REGION
#     TF_STATE_BUCKET
#     TF_LOCK_TABLE
#     IAM_USER
#     SERVICE_ACCOUNT_NAME  — ESO service account name
#
#   Environment secrets (dev / stage / prod):
#     AWS_ACCOUNT_ID
#
# ── Helm charts ───────────────────────────────────────────────────────────────
# Helm charts live in ./charts in this same repo.
# The update-helm job commits changes directly — no PAT or second checkout needed.
#
# ─────────────────────────────────────────────────────────────────────────────

name: "Infrastructure"

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        type: choice
        options:
          - dev
          - stage
          - prod
      action:
        description: "Terraform action"
        required: true
        type: choice
        options:
          - plan
          - apply
          - destroy
        default: plan
      confirm_destroy:
        description: "Type DESTROY to confirm (destroy only)"
        required: false
        type: string
        default: ""

permissions:
  id-token: write
  contents: write  # needed to commit helm chart updates back to this repo

env:
  TF_VERSION: "1.11.1"
  TF_IN_AUTOMATION: "true"
  TF_INPUT: "false"
  INFRA_DIR: "./infrastructure/infra"
  ARGOCD_DIR: "./infrastructure/argocd"

# ── Jobs ───────────────────────────────────────────────────────────────────────

jobs:

  # ── Guard ──────────────────────────────────────────────────────────────────
  guard:
    name: "Guard"
    runs-on: ubuntu-latest
    steps:
      - name: Validate destroy confirmation
        if: github.event.inputs.action == 'destroy'
        run: |
          if [ "${{ github.event.inputs.confirm_destroy }}" != "DESTROY" ]; then
            echo "::error::Destroy requires typing DESTROY in the confirmation field."
            echo "You typed: '${{ github.event.inputs.confirm_destroy }}'"
            exit 1
          fi
          echo "Destroy confirmed."

      - name: Run summary
        run: |
          echo "## Infrastructure Run" >> $GITHUB_STEP_SUMMARY
          echo "| | |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| **Environment** | \`${{ github.event.inputs.environment }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Action** | \`${{ github.event.inputs.action }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Triggered by** | ${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Run ID** | ${{ github.run_id }} |" >> $GITHUB_STEP_SUMMARY

  # ── Infra Plan ─────────────────────────────────────────────────────────────
  infra-plan:
    name: "Infra Plan — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: guard
    environment: ${{ github.event.inputs.environment }}

    outputs:
      plan_exit_code: ${{ steps.plan.outputs.exit_code }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: infra-plan-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Generate ci-secrets.tfvars
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          cat > ci-secrets.tfvars << EOF
          aws_account_id = "${{ secrets.AWS_ACCOUNT_ID }}"
          iam_user = "${{ secrets.IAM_USER }}"
          EOF

      - name: Terraform Init
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=infra/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: Select workspace
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          terraform workspace select ${{ github.event.inputs.environment }} \
            || terraform workspace new ${{ github.event.inputs.environment }}

      - name: Terraform Validate
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform validate

      - name: Terraform Plan
        id: plan
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          ENV="${{ github.event.inputs.environment }}"
          DESTROY_FLAG=""
          [[ "${{ github.event.inputs.action }}" == "destroy" ]] && DESTROY_FLAG="-destroy"

          terraform plan \
            $DESTROY_FLAG \
            -var-file="terraform.${ENV}.tfvars" \
            -var-file="ci-secrets.tfvars" \
            -out=tfplan \
            -detailed-exitcode \
            -no-color 2>&1 | tee plan_output.txt

          # Exit codes: 0 = no changes, 1 = error, 2 = changes pending
          EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          [ "$EXIT_CODE" -eq 1 ] && exit 1
          echo "Plan complete (exit code $EXIT_CODE)."

      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: infra-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: |
            ${{ env.INFRA_DIR }}/tfplan
            ${{ env.INFRA_DIR }}/plan_output.txt
          retention-days: 1

      - name: Plan summary
        run: |
          EXIT_CODE="${{ steps.plan.outputs.exit_code }}"
          echo "### Infra Plan — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          [ "$EXIT_CODE" == "0" ] && echo "✅ No infrastructure changes." >> $GITHUB_STEP_SUMMARY
          [ "$EXIT_CODE" == "2" ] && echo "⚠️ Changes pending — review before applying." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "<details><summary>Plan output</summary>" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat ${{ env.INFRA_DIR }}/plan_output.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY

  # ── Infra Apply ────────────────────────────────────────────────────────────
  infra-apply:
    name: "Infra Apply — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: infra-plan
    environment: ${{ github.event.inputs.environment }}
    if: |
      github.event.inputs.action == 'apply' &&
      needs.infra-plan.outputs.plan_exit_code == '2'

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: infra-apply-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=infra/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: Select workspace
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform workspace select ${{ github.event.inputs.environment }}

      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: infra-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: ${{ env.INFRA_DIR }}

      - name: Terraform Apply
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform apply -auto-approve tfplan 2>&1 | tee apply_output.txt

      - name: Apply summary
        if: success()
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          echo "### ✅ Infra Apply Complete — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          terraform output -no-color 2>/dev/null || echo "No outputs."
          echo '```' >> $GITHUB_STEP_SUMMARY

  # ── Update Helm ────────────────────────────────────────────────────────────
  # Helm charts live in ./charts in this same repo.
  # Outputs are pulled directly from S3 state — no artifact passing needed.
  #
  # Runs when action == apply AND infra-plan succeeded (even if infra-apply was
  # skipped because there were no infra changes — exit code 0). Without always()
  # here, a skipped infra-apply would cascade and skip this job too.
  update-helm:
    name: "Update Helm — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: [infra-plan, infra-apply]
    if: |
      always() &&
      github.event.inputs.action == 'apply' &&
      needs.infra-plan.result == 'success' &&
      needs.infra-plan.outputs.plan_exit_code != '1' &&
      (needs.infra-apply.result == 'success' || needs.infra-apply.result == 'skipped')

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ github.token }}  # needs contents: write to push

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: helm-update-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Extract Terraform outputs
        run: |
          chmod +x .github/scripts/extract-tf-outputs.sh
          .github/scripts/extract-tf-outputs.sh \
            "${{ secrets.TF_STATE_BUCKET }}" \
            "${{ github.event.inputs.environment }}" \
            "infra/terraform.tfstate"

      - name: Update Helm chart values
        run: |
          chmod +x .github/scripts/update-helm.sh
          .github/scripts/update-helm.sh \
            "${{ github.event.inputs.environment }}" \
            "${{ secrets.AWS_REGION }}" \
            "${{ env.IRSA_ARN }}" \
            "${{ env.FRONTEND_REPO_URL }}" \
            "${{ env.BACKEND_REPO_URL }}" \
            "./charts"

  # ── ArgoCD Plan ────────────────────────────────────────────────────────────
  argocd-plan:
    name: "ArgoCD Plan — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: update-helm
    environment: ${{ github.event.inputs.environment }}
    if: |
      always() &&
      needs.update-helm.result == 'success'

    outputs:
      plan_exit_code: ${{ steps.plan.outputs.exit_code }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: argocd-plan-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Generate ci-secrets.tfvars
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          cat > ci-secrets.tfvars << EOF
          aws_account_id = "${{ secrets.AWS_ACCOUNT_ID }}"
          EOF

      - name: Terraform Init
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=argocd/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: Select workspace
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          terraform workspace select ${{ github.event.inputs.environment }} \
            || terraform workspace new ${{ github.event.inputs.environment }}

      - name: Terraform Validate
        working-directory: ${{ env.ARGOCD_DIR }}
        run: terraform validate

      - name: Terraform Plan
        id: plan
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          ENV="${{ github.event.inputs.environment }}"

          terraform plan \
            -var-file="terraform.${ENV}.tfvars" \
            -var-file="ci-secrets.tfvars" \
            -out=tfplan \
            -detailed-exitcode \
            -no-color 2>&1 | tee plan_output.txt

          EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          [ "$EXIT_CODE" -eq 1 ] && exit 1
          echo "Plan complete (exit code $EXIT_CODE)."

      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: argocd-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: |
            ${{ env.ARGOCD_DIR }}/tfplan
            ${{ env.ARGOCD_DIR }}/plan_output.txt
          retention-days: 1

      - name: Plan summary
        run: |
          EXIT_CODE="${{ steps.plan.outputs.exit_code }}"
          echo "### ArgoCD Plan — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          [ "$EXIT_CODE" == "0" ] && echo "✅ No ArgoCD changes." >> $GITHUB_STEP_SUMMARY
          [ "$EXIT_CODE" == "2" ] && echo "⚠️ Changes pending." >> $GITHUB_STEP_SUMMARY
          echo "<details><summary>Plan output</summary>" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat ${{ env.ARGOCD_DIR }}/plan_output.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY

  # ── ArgoCD Apply ───────────────────────────────────────────────────────────
  argocd-apply:
    name: "ArgoCD Apply — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: argocd-plan
    environment: ${{ github.event.inputs.environment }}
    if: |
      always() &&
      needs.argocd-plan.result == 'success' &&
      needs.argocd-plan.outputs.plan_exit_code == '2'

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: argocd-apply-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=argocd/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: Select workspace
        working-directory: ${{ env.ARGOCD_DIR }}
        run: terraform workspace select ${{ github.event.inputs.environment }}

      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: argocd-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: ${{ env.ARGOCD_DIR }}

      - name: Terraform Apply
        working-directory: ${{ env.ARGOCD_DIR }}
        run: terraform apply -auto-approve tfplan 2>&1 | tee apply_output.txt

      - name: Apply summary
        if: success()
        run: |
          echo "### ✅ ArgoCD Apply Complete — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY

  # ── Destroy ────────────────────────────────────────────────────────────────
  # Order matters: ArgoCD (helm_release app-of-apps) is torn down FIRST so
  # ArgoCD stops reconciling child apps before the EKS cluster is deleted.
  # Destroying infra while ArgoCD is still managing apps causes stuck finalizers.
  #
  # Phase 1 — ArgoCD: terraform destroy (direct, no pre-planned artifact)
  # Phase 2 — Infra:  terraform apply the pre-planned destroy tfplan from infra-plan
  destroy:
    name: "Destroy — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: infra-plan
    environment: ${{ github.event.inputs.environment }}
    if: |
      github.event.inputs.action == 'destroy' &&
      github.event.inputs.confirm_destroy == 'DESTROY' &&
      needs.infra-plan.outputs.plan_exit_code == '2'

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: destroy-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      # ── Phase 1: Destroy ArgoCD first ──────────────────────────────────────
      # Removes the helm_release (app-of-apps) and lets ArgoCD clean up child
      # apps before the cluster disappears underneath them.

      - name: "[ArgoCD] Generate ci-secrets.tfvars"
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          cat > ci-secrets.tfvars << EOF
          aws_account_id = "${{ secrets.AWS_ACCOUNT_ID }}"
          iam_user = "${{ secrets.IAM_USER }}"
          EOF

      - name: "[ArgoCD] Terraform Init"
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=argocd/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: "[ArgoCD] Select workspace"
        working-directory: ${{ env.ARGOCD_DIR }}
        run: terraform workspace select ${{ github.event.inputs.environment }}

      - name: "[ArgoCD] Terraform Destroy"
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          ENV="${{ github.event.inputs.environment }}"
          terraform destroy -auto-approve \
            -var-file="terraform.${ENV}.tfvars" \
            -var-file="ci-secrets.tfvars" \
            2>&1 | tee argocd_destroy_output.txt

      # ── Phase 2: Destroy Infra ─────────────────────────────────────────────
      # Uses the pre-planned destroy tfplan from the infra-plan job.
      # EKS, VPC, RDS, ECR, IAM — everything.

      - name: "[Infra] Generate ci-secrets.tfvars"
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          cat > ci-secrets.tfvars << EOF
          aws_account_id = "${{ secrets.AWS_ACCOUNT_ID }}"
          EOF

      - name: "[Infra] Terraform Init"
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=infra/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: "[Infra] Select workspace"
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform workspace select ${{ github.event.inputs.environment }}

      - name: "[Infra] Download destroy plan artifact"
        uses: actions/download-artifact@v4
        with:
          name: infra-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: ${{ env.INFRA_DIR }}

      - name: "[Infra] Terraform Destroy"
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform apply -auto-approve tfplan 2>&1 | tee infra_destroy_output.txt

      - name: Destroy summary
        if: always()
        run: |
          echo "### 💀 Destroy Complete — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Phase | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| ArgoCD (app-of-apps) | destroyed first ✓ |" >> $GITHUB_STEP_SUMMARY
          echo "| Infrastructure (EKS/VPC/RDS) | destroyed second ✓ |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Triggered by: **${{ github.actor }}**" >> $GITHUB_STEP_SUMMARY

===== FILE: ./infrastructure/argocd/app/Chart.yaml =====

apiVersion: v2
name: root
description: A Helm chart for argocd app of apps

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"


===== FILE: ./infrastructure/argocd/app/.helmignore =====

# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/


===== FILE: ./infrastructure/argocd/apps/Chart.yaml =====

apiVersion: v2
name: apps
description: A Helm chart for Kubernetes

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"


===== FILE: ./infrastructure/argocd/apps/.helmignore =====

# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/


===== FILE: ./infrastructure/argocd/apps/templates/app/backend.yaml =====

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.app.syncWaves | quote }}
  #  argocd.argoproj.io/hook: {{ .Values.app.hook }}
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    targetRevision: {{ .Values.targetRevision }}
    path: {{ .Values.app.backend.path }}
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - Validate=false
    - ServerSideApply=true
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m


===== FILE: ./infrastructure/argocd/apps/templates/app/frontend.yaml =====

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.app.syncWaves | quote }}
  #  argocd.argoproj.io/hook: {{ .Values.app.hook }}
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    targetRevision: {{ .Values.targetRevision }}
    path: {{ .Values.app.frontend.path }}
  destination:
    server: https://kubernetes.default.svc
    namespace: dev 
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - Validate=false
    - ServerSideApply=true
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m


===== FILE: ./infrastructure/argocd/apps/templates/controllers-crds/eso.yaml =====

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.esoCrd.name }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.esoCrd.syncWaves | quote }}
    argocd.argoproj.io/hook: {{ .Values.esoCrd.hook }}
spec:
  project: default
  source:
    repoURL: {{ .Values.esoCrd.repoURL }}
    chart: {{ .Values.esoCrd.chart }}
    targetRevision: {{ .Values.esoCrd.version }}
    helm:
      values: |
        serviceAccount:
          create: false
          name: external-secrets-sa

  destination:
    server: https://kubernetes.default.svc 
    namespace: {{ .Values.esoCrd.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - Validate=false
    - ServerSideApply=true
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

===== FILE: ./infrastructure/argocd/apps/templates/controllers-crds/gateway-api.yaml =====

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.gatewayCrd.name }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.gatewayCrd.syncWaves | quote }}
    argocd.argoproj.io/hook: {{ .Values.gatewayCrd.hook }}
spec:
  project: default
  source:
    repoURL: {{ .Values.gatewayCrd.repoURL }}
    chart: {{ .Values.gatewayCrd.chart }}
    targetRevision: {{ .Values.gatewayCrd.version }}
 #   helm:
 #     values: |

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.gatewayCrd.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - Validate=false
    - ServerSideApply=true
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

===== FILE: ./infrastructure/argocd/apps/templates/resources/argocd.yaml =====

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.argocdCr.name }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.argocdCr.syncWaves | quote }}
    argocd.argoproj.io/hook: {{ .Values.argocdCr.hook }}
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    path: {{ .Values.argocdCr.path }}

  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=false
    - Validate=false
    - ServerSideApply=true
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

===== FILE: ./infrastructure/argocd/apps/templates/resources/eso.yaml =====

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.esoCr.name }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.esoCr.syncWaves | quote }}
    argocd.argoproj.io/hook: {{ .Values.esoCr.hook }}
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    path: {{ .Values.esoCr.path }}

  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=false
    - Validate=false
    - ServerSideApply=true
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

===== FILE: ./infrastructure/argocd/apps/templates/resources/gateway-api.yaml =====

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.gatewayCr.name }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.gatewayCr.syncWaves | quote }}
    argocd.argoproj.io/hook: {{ .Values.gatewayCr.hook }}
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    path: {{ .Values.gatewayCr.path }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.gatewayCr.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - Validate=false
    - ServerSideApply=true
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

===== FILE: ./infrastructure/argocd/apps/values.yaml =====

repoURL: "https://github.com/jamiekariuki/EKS-External-Secrets-Operator.git"

targetRevision: main
#=======================================================
#                      app   
#======================================================

app:
  syncWaves: "1"
  frontend:
    path: charts/frontend  
  backend:
    path: charts/backend
   

#=======================================================
#                      controllers & CRDs 
#=======================================================

gatewayCrd:
  name: gateway-api-crd
  namespace: envoy-gateway-system
  repoURL: oci://docker.io/envoyproxy/gateway-helm
  chart: gateway-helm
  version: "v1.6.1"
  hook: PreSync
  syncWaves: "-10"

esoCrd:
  name: eso-crd
  namespace: external-secrets
  repoURL: https://charts.external-secrets.io
  chart: external-secrets
  version: "v1.1.0"
  hook: PreSync
  syncWaves: "-10"
  serviceAccount:
    name: external-secrets-sa
    irsaArn: 
#=======================================================
#                      resources                       
#=======================================================
gatewayCr:
  name: gateway-api-cr
  namespace: gateway
  path: charts/gateway
  hook: Sync
  syncWaves: "-9" #2nd

argocdCr:
  name: argocd-cr
  path: charts/argocd
  hook: Sync
  syncWaves: "-8"

esoCr:
  name: eso-cr
  path: charts/eso
  hook: Sync
  syncWaves: "-8"


===== FILE: ./infrastructure/argocd/app/templates/root.yaml =====

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.app.name }}
  namespace: argocd
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    targetRevision: {{ .Values.targetRevision }}
    path: {{ .Values.app.path }}
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

   



===== FILE: ./infrastructure/argocd/app/values.yaml =====

repoURL: "https://github.com/jamiekariuki/EKS-External-Secrets-Operator.git"

targetRevision: main

app:
  name: root-app
  path: infrastructure/argocd/apps




 

===== FILE: ./infrastructure/argocd/argocd.tf =====

 #app of apps
resource "helm_release" "app_of_apps" {

  name       = "root-app"
  chart      = "${path.module}/app"
  namespace  = "argocd"

  values = [
    file("${path.module}/app/values.yaml")
  ]
}







===== FILE: ./infrastructure/argocd/backend.tf =====

terraform {
  backend "s3" {
    bucket = "466872246229-state"
    key = "networking/terraform.tfstate"
    region = "us-east-1"
    dynamodb_table = "terraform-locks"
    encrypt = true
  }
}

===== FILE: ./infrastructure/argocd/locals.tf =====

locals {
  name_prefix = "${var.project}-${var.ENV_PREFIX}"

  common_tags = {
    Project     = var.project
    Environment = var.ENV_PREFIX
    Terraform   = "true"
    ManagedBy   = "terraform"
  }
}  

===== FILE: ./infrastructure/argocd/provider.tf =====

provider "aws" {
  region              = var.region
  allowed_account_ids = [var.aws_account_id]
}

# Get cluster info
data "aws_eks_cluster" "default" {
  name = local.name_prefix
}

# Get authentication token
data "aws_eks_cluster_auth" "default" {
  name = local.name_prefix
}

# Kubernetes provider
provider "kubernetes" {
  host                   = data.aws_eks_cluster.default.endpoint
  cluster_ca_certificate = base64decode(data.aws_eks_cluster.default.certificate_authority[0].data)
  token                  = data.aws_eks_cluster_auth.default.token
}

# Helm provider
provider "helm" {
  kubernetes ={
    host                   = data.aws_eks_cluster.default.endpoint
    cluster_ca_certificate = base64decode(data.aws_eks_cluster.default.certificate_authority[0].data)
    token                  = data.aws_eks_cluster_auth.default.token
  }
}

#test


===== FILE: ./infrastructure/argocd/terraform.dev.tfvars =====

region         = "us-east-1"
ENV_PREFIX= "dev"
project = "eso"



===== FILE: ./infrastructure/argocd/terraform.prod.tfvars =====

region         = "us-east-1"
ENV_PREFIX= "prod"
project = "eso"



===== FILE: ./infrastructure/argocd/terraform.stage.tfvars =====

region         = "us-east-1"
ENV_PREFIX= "stage"
project = "eso"



===== FILE: ./infrastructure/argocd/variables.tf =====

variable "aws_account_id" {
  type = string
}

variable "region" {
  type = string
}

variable "project" {
  type = string
  default = "crud"
}

variable "ENV_PREFIX" {
    type = string

    validation {
      condition = contains(["dev", "stage", "prod"], var.ENV_PREFIX)
      error_message = "provide an environment"
    }

    default = "dev"
}





===== FILE: ./infrastructure/.gitignore =====

# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
terraform.tfvars
.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# tempts
scrap.sh
scrap-content.txt
temp.txt
deployment.md

===== FILE: ./infrastructure/infra/backend.tf =====


terraform {
  backend "s3" {}
}


===== FILE: ./infrastructure/infra/ecr.tf =====

# ── ECR Repository ─────────────────────────────────────────────
# Shared across dev and prod — created once, never re-created.
# CI pushes "latest" here; both dev and prod ECS pull from this repo.


module "ecr_frontend" {
  source  = "terraform-aws-modules/ecr/aws"
  version = "3.2.0"

  repository_name = "${var.project}-frontend"

  # MUTABLE — every push overwrites "latest".
  repository_image_tag_mutability = "MUTABLE"

  # Scan every image on push for CVEs.
  repository_image_scan_on_push = true

  # Never force-delete — this repo holds production images.
  repository_force_delete = true

  repository_lifecycle_policy = jsonencode({
    rules = [
      {
        # Each push overwrites "latest" and orphans the previous image
        # (it becomes untagged). Clean those up after 1 day.
        rulePriority = 1
        description  = "Delete untagged (superseded) images after 1 day"
        selection = {
          tagStatus   = "untagged"
          countType   = "sinceImagePushed"
          countUnit   = "days"
          countNumber = 1
        }
        action = { type = "expire" }
      }
    ]
  })

  tags = {
    Project   = var.project
    Terraform = "true"
    ManagedBy = "terraform"
    Shared    = "true"
  }
}


# ── ECR Repository ─────────────────────────────────────────────
# Shared across dev and prod — created once, never re-created.
# CI pushes "latest" here; both dev and prod ECS pull from this repo.


module "ecr_backend" {
  source  = "terraform-aws-modules/ecr/aws"
  version = "3.2.0"

  repository_name = "${var.project}-backend"

  # MUTABLE — every push overwrites "latest".
  repository_image_tag_mutability = "MUTABLE"

  # Scan every image on push for CVEs.
  repository_image_scan_on_push = true

  # Never force-delete — this repo holds production images.
  repository_force_delete = true

  repository_lifecycle_policy = jsonencode({
    rules = [
      {
        # Each push overwrites "latest" and orphans the previous image
        # (it becomes untagged). Clean those up after 1 day.
        rulePriority = 1
        description  = "Delete untagged (superseded) images after 1 day"
        selection = {
          tagStatus   = "untagged"
          countType   = "sinceImagePushed"
          countUnit   = "days"
          countNumber = 1
        }
        action = { type = "expire" }
      }
    ]
  })

  tags = {
    Project   = var.project
    Terraform = "true"
    ManagedBy = "terraform"
    Shared    = "true"
  }
}


output "frontend_repository_url" {
  value = module.ecr_frontend.repository_url
}

output "backend_repository_url" {
  value = module.ecr_backend.repository_url
}

===== FILE: ./infrastructure/infra/eks.tf =====

 //kms encryption for eks secrets in etcd
resource "aws_kms_key" "k8s_encryption" {
  description             = "KMS key for encrypting EKS secrets"
  deletion_window_in_days = 10
  enable_key_rotation     = true
}


//eks
module "eks" {
 // depends_on = [ module.aws-iam-identity-center ]
  source  = "terraform-aws-modules/eks/aws"
  version = "~> 21.0"

  name               = local.name_prefix
  kubernetes_version = "1.33"

  addons = {
    coredns                = {}
    eks-pod-identity-agent = {
      before_compute = true
    }
    kube-proxy             = {}
    vpc-cni                = {
      before_compute = true
    }
  }

  encryption_config = {
    provider_key_arn = aws_kms_key.k8s_encryption.arn
    resources        = ["secrets"]
  }
  
  endpoint_public_access = true

  # Optional: Adds the current caller identity as an administrator via cluster access entry
  enable_cluster_creator_admin_permissions = true

  vpc_id                   = module.vpc.vpc_id
  subnet_ids               = module.vpc.private_subnets
  control_plane_subnet_ids = module.vpc.intra_subnets

  # EKS Managed Node Group(s)
  eks_managed_node_groups = {
    example = {
      ami_type       = "AL2023_x86_64_STANDARD"
      instance_types = ["t3a.xlarge", "t3.xlarge", "t3a.2xlarge"]

      capacity_type = "SPOT"

      min_size     = 1
      max_size     = 3
      desired_size = 1
    }
  }

  access_entries = {
/*    github = {
      principal_arn = aws_iam_role.github_oidc_provider_aws.arn
    }
*/
    # One access entry with a policy associated
    example = {
      principal_arn = var.iam_user

      policy_associations = {
        example = {
          policy_arn = "arn:aws:eks::aws:cluster-access-policy/AmazonEKSClusterAdminPolicy"
          access_scope = {
            type = "cluster"
          }
        }
      }
    }
  }  

  tags = local.common_tags
}



//helm resource for argocd  (installing argocd)
 resource "helm_release" "argocd" {
  depends_on = [ module.eks ]
  name             = "argocd"
  repository       = "https://argoproj.github.io/argo-helm"
  chart            = "argo-cd"
  version          = "9.1.4"
  namespace        = "argocd"
  create_namespace = true

  values = [
    yamlencode({
      server = {
        service = {
          type = "LoadBalancer"
          annotations = {
            "service.beta.kubernetes.io/aws-load-balancer-scheme" = "internet-facing"
          }
        }
      }
    })
  ]  
} 




===== FILE: ./infrastructure/infra/iam.tf =====

  //policy for eks to read ecr
data "aws_iam_policy" "read_ecr" {
  arn = "arn:aws:iam::aws:policy/AmazonEC2ContainerRegistryReadOnly"
}
//attach policy
resource "aws_iam_role_policy_attachment" "ecr_attachment" {
  role = module.eks.eks_managed_node_groups["example"].iam_role_name
  policy_arn = data.aws_iam_policy.read_ecr.arn
}

  

===== FILE: ./infrastructure/infra/irsa.tf =====

# Namespace for ESO (skip this resource if it already exists / is managed elsewhere)
resource "kubernetes_namespace" "external_secrets" {
  metadata {
    name = "external-secrets"
  }
}

# The actual k8s ServiceAccount that ESO's pod will use.
# The annotation is what lets EKS's webhook inject the IRSA credentials
# (AWS_ROLE_ARN + AWS_WEB_IDENTITY_TOKEN_FILE) into the pod.
resource "kubernetes_service_account" "external_secrets" {
  metadata {
    name      = "external-secrets-sa"
    namespace = kubernetes_namespace.external_secrets.metadata[0].name

    annotations = {
      "eks.amazonaws.com/role-arn" = module.external_secrets_irsa.arn
    }
  }
}
 
module "external_secrets_irsa" {
  source  = "terraform-aws-modules/iam/aws//modules/iam-role-for-service-accounts"
  version = "6.6.1"

  name = "external-secrets-irsa"

  attach_external_secrets_policy        = true
  external_secrets_secrets_manager_arns = [module.secret_rds_credentials.secret_arn]

  oidc_providers = {
    this = {
      provider_arn               = module.eks.oidc_provider_arn
      namespace_service_accounts = ["external-secrets:external-secrets-sa"]
    }
  }

  tags = local.common_tags
}

output "irsa_arn" {
  value = module.external_secrets_irsa.arn
}

===== FILE: ./infrastructure/infra/locals.tf =====

locals {
  name_prefix = "${var.project}-${var.ENV_PREFIX}"

  common_tags = {
    Project     = var.project
    Environment = var.ENV_PREFIX
    Terraform   = "true"
    ManagedBy   = "terraform"
  }
}  

===== FILE: ./infrastructure/infra/provider.tf =====

provider "aws" {
  region              = var.region
  allowed_account_ids = [var.aws_account_id]
}

terraform {

  required_version = ">= 1.11.1"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 6.34"
    }
    random = {
      source  = "hashicorp/random"
      version = ">= 3.7"
    }
  }
}



# Get cluster info
data "aws_eks_cluster" "this" {
  name = module.eks.cluster_name

  depends_on = [module.eks]
}

# Get authentication token
data "aws_eks_cluster_auth" "this" {
  name = module.eks.cluster_name

  depends_on = [module.eks]
}

# Kubernetes provider
provider "kubernetes" {
  host                   = data.aws_eks_cluster.this.endpoint
  cluster_ca_certificate = base64decode(data.aws_eks_cluster.this.certificate_authority[0].data)
  token                  = data.aws_eks_cluster_auth.this.token

}

# Helm provider
provider "helm" {
  kubernetes ={
    host                   = data.aws_eks_cluster.this.endpoint
    cluster_ca_certificate = base64decode(data.aws_eks_cluster.this.certificate_authority[0].data)
    token                  = data.aws_eks_cluster_auth.this.token
  }
}
 



===== FILE: ./infrastructure/infra/rds.tf =====

locals {
  db_username = "crudapp"
  db_name     = "crud"
  db_port     = 5432
}

resource "random_password" "rds" {
  length  = 32
  special = false
}

module "rds_crud" {
  source  = "terraform-aws-modules/rds/aws"
  version = "7.1.0"

  identifier = "${local.name_prefix}-postgres"

  engine         = "postgres"
  engine_version = "14"
  family         = "postgres14"
  instance_class = "db.t3.micro"

  allocated_storage     = 20
  max_allocated_storage = 100
  storage_type          = "gp3"
  storage_encrypted     = true

  db_name  = local.db_name
  username = local.db_username
  port     = local.db_port

  manage_master_user_password = false
  password_wo                 = random_password.rds.result
  password_wo_version         = 1

  create_db_subnet_group = true
  subnet_ids             = module.vpc.private_subnets
  db_subnet_group_name   = "${local.name_prefix}-postgres"

  vpc_security_group_ids = [module.db_sg.security_group_id]
  publicly_accessible    = false

  parameters = [
    {
      name         = "rds.force_ssl"
      value        = "1"
      apply_method = "pending-reboot"
    }
  ]

  create_db_option_group = false

  multi_az          = false
  availability_zone = "${var.region}a"

  backup_retention_period = 7
  backup_window           = "03:00-04:00"
  maintenance_window      = "Mon:04:00-Mon:05:00"

  auto_minor_version_upgrade  = true
  allow_major_version_upgrade = false

  enabled_cloudwatch_logs_exports        = ["postgresql"]
  create_cloudwatch_log_group            = true
  cloudwatch_log_group_retention_in_days = 30

  deletion_protection = var.ENV_PREFIX == "prod" ? true : false
  skip_final_snapshot = var.ENV_PREFIX == "prod" ? false : true

  final_snapshot_identifier_prefix = "${local.name_prefix}-final"
  delete_automated_backups         = false

  tags = local.common_tags
}



===== FILE: ./infrastructure/infra/secrets-manager.tf =====

# Single secret holding everything an app needs to connect to RDS.
# ESO syncs this whole JSON blob into a Kubernetes Secret — see eso.tf.

module "secret_rds_credentials" {
  source  = "terraform-aws-modules/secrets-manager/aws"
  version = "2.1.0"

  name                    = "${local.name_prefix}/rds/credentials"
  description             = "RDS connection credentials for ${local.name_prefix}"
  recovery_window_in_days = var.ENV_PREFIX == "prod" ? 30 : 0

  secret_string = jsonencode({
    username     = local.db_username
    password     = random_password.rds.result
    host         = module.rds_crud.db_instance_address 
    port         = local.db_port
    dbname       = local.db_name
    engine       = "postgres"
    database_url = "postgresql://${local.db_username}:${random_password.rds.result}@${module.rds_crud.db_instance_address}:${local.db_port}/${local.db_name}"
  })

  ignore_secret_changes = false
  tags                   = local.common_tags
}

output "rds_secret_arn" {
  value     = module.secret_rds_credentials.secret_arn
  sensitive = false # the ARN itself isn't sensitive, the value inside it is
}

===== FILE: ./infrastructure/infra/security-groups.tf =====


// Security Group for RDS
module "db_sg" {
  source  = "terraform-aws-modules/security-group/aws"
  version = "~> 5.0"

  name        = "${var.ENV_PREFIX}-db-sg"
  description = "PostgreSQL security group"
  vpc_id      = module.vpc.vpc_id  # MUST match DB subnets

  ingress_with_cidr_blocks = [
    {
      from_port   = 5432
      to_port     = 5432
      protocol    = "tcp"
      description = "PostgreSQL access from within VPC"
      cidr_blocks = module.vpc.vpc_cidr_block 
    }
  ]

  tags = {
    Environment = var.ENV_PREFIX
    Terraform   = "true"
  }
}

===== FILE: ./infrastructure/infra/terraform.dev.tfvars =====

region         = "us-east-1"
ENV_PREFIX= "dev"
project = "eso"



===== FILE: ./infrastructure/infra/terraform.prod.tfvars =====

region         = "us-east-1"
ENV_PREFIX= "prod"
project = "eso"



===== FILE: ./infrastructure/infra/terraform.stage.tfvars =====

region         = "us-east-1"
ENV_PREFIX= "stage"
project = "eso"



===== FILE: ./infrastructure/infra/terraform.tfvars =====

region         = "us-east-1"
aws_account_id = "290488660393"
ENV_PREFIX= "dev"

iam_user= "arn:aws:iam::290488660393:user/karis"


project = "eso"


#accessing eks
#   aws eks update-kubeconfig --name eso-dev --region us-east-1 --profile karis
#   kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d && echo
# 2Huf4B2Jkfm4DhhY

#change aws
# export AWS_PROFILE=prod

#protfoward
# kubectl port-forward svc/argocd-server -n argocd 8080:80

/* 
aws ecr batch-delete-image \
  --repository-name frontend-repository \
  --image-ids "$(aws ecr list-images --repository-name frontend-repository --query 'imageIds' --output json)"


aws ecr batch-delete-image \
  --repository-name backend-repository \
  --image-ids "$(aws ecr list-images --repository-name backend-repository --query 'imageIds' --output json)"
 */

#enable aws organization
/*
create
  aws organizations create-organization --feature-set ALL
confirm
  aws organizations describe-organization
enable identy center
  aws sso-admin create-instance
cofirm
  aws sso-admin list-instances


*/

===== FILE: ./infrastructure/infra/variables.tf =====

variable "aws_account_id" {
  type = string
}

variable "region" {
  type = string
}

variable "project" {
  type = string
  default = "crud"
}

variable "iam_user" {
  type = string
}

variable "ENV_PREFIX" {
    type = string

    validation {
      condition = contains(["dev", "stage", "prod"], var.ENV_PREFIX)
      error_message = "provide an environment"
    }

    default = "dev"
}

===== FILE: ./infrastructure/infra/vpc.tf =====

//vpc
 module "vpc" {
  source = "terraform-aws-modules/vpc/aws"

  name = local.name_prefix
  cidr = "10.0.0.0/16"

  azs             = ["${var.region}a", "${var.region}b", "${var.region}c"]
  private_subnets = ["10.0.1.0/24", "10.0.2.0/24", "10.0.3.0/24"]
  public_subnets  = ["10.0.101.0/24", "10.0.102.0/24", "10.0.103.0/24"]
  intra_subnets   = ["10.0.104.0/24", "10.0.105.0/24", "10.0.106.0/24"]

  enable_nat_gateway = true
  single_nat_gateway = true
  one_nat_gateway_per_az = false
  enable_dns_hostnames = true
  
  tags = local.common_tags

  public_subnet_tags = {
    "kubernetes.io/cluster/${local.name_prefix}"= "shared"
    "kubernetes.io/role/elb"= 1
  }
  private_subnet_tags = {
    "kubernetes.io/cluster/${local.name_prefix}"= "shared"
    "kubernetes.io/role/internal-elb"= 1
  }
} 

===== FILE: ./infrastructure/OIDC/main.tf =====

module "github-oidc" {
  source  = "terraform-module/github-oidc-provider/aws"
  version = "~> 1"

  create_oidc_provider = true
  create_oidc_role     = true

  repositories = var.repository

  oidc_role_attach_policies = var.policies
}


===== FILE: ./infrastructure/OIDC/outputs.tf =====

/* output "oidc_provider_arn" {
  description = "OIDC provider ARN"
  value       = module.github-oidc.oidc_provider_arn
} */

output "github_oidc_role" {
  description = "CICD GitHub role."
  value       = module.github-oidc.oidc_role
}

===== FILE: ./infrastructure/OIDC/provider.tf =====

provider "aws" {
  region              = var.region
  allowed_account_ids = [var.aws_account_id]
}




===== FILE: ./infrastructure/OIDC/.terraform.lock.hcl =====

# This file is maintained automatically by "terraform init".
# Manual edits may be lost in future updates.

provider "registry.terraform.io/hashicorp/aws" {
  version = "6.51.0"
  hashes = [
    "h1:bclp+xS1fYeOCil0XZO6mKvEeHFESt5K/XotVSZND54=",
    "zh:03fcea0a1ea2ca81d62d4d2e2961181bef9068b1c701f2cddc4aa5fac105818a",
    "zh:1213944cd623143974ea5c9b70b22ae1ccca33d743924c149ed089d34b8e08b4",
    "zh:190a46da0c69082b74da48238ce134d2fc9893e09122ac249c5689f88eab7e13",
    "zh:1b312a4b53fa3cf731f95e674c033865feea5455f163b86136f2614424637293",
    "zh:2b319814806222c5aba196b1a78756a6b36dc5c91f85edda349234d8a2f20a6a",
    "zh:2bddf92c8efc6ad445a2eb8a0e5f88742a0596392c3a4ebc350ebb4105a4a96d",
    "zh:3bef0c4f675c09034ff017cf899977b1765b2c0b3d1e489bcb06a5fcac316e2d",
    "zh:47c46b5aa22199638fed5c93b195bbfd1182a1408edad4e5c39d4a73a04493f6",
    "zh:5f808699650f6db961964466c77f5a581eab142a91c2e54810bb09b6f2fcd3f2",
    "zh:9b12af85486a96aedd8d7984b0ff811a4b42e3d88dad1a3fb4c0b580d04fa425",
    "zh:ada97e6be10164f452e278c23412b8597698a9c95ffb68fe83629d63d85906f3",
    "zh:c4d73a91810d8dbcf9abbd431d41fcceebb48f8b6fd3c28a84bb3c6ed08be2e9",
    "zh:c63ec875d38fc557b16b0b2b0ab1c7635852799453113240e21a52409de94a71",
    "zh:cdd0209a755fc3aa14855aa013dae4b166a2fc7f6d3cbb673f7ff2142f5b63a2",
    "zh:e5e665a27290391fd1bffc093ab68b596f6c507785be2e3f0949fab4fd6aec1b",
    "zh:f6c42046a31d65eff2793737656b38931f90318b53661046bb84326cd4cb558f",
  ]
}


===== FILE: ./infrastructure/OIDC/terraform.tfvars =====

repository = [
    "jamiekariuki/EKS-External-Secrets-Operator:ref:refs/heads/main",
    "jamiekariuki/EKS-External-Secrets-Operator:environment:dev",
    "jamiekariuki/EKS-External-Secrets-Operator:environment:stage",
    "jamiekariuki/EKS-External-Secrets-Operator:environment:prod",
]
branch = "main"
region = "us-east-1"
aws_account_id = "290488660393"
policies = ["arn:aws:iam::aws:policy/AdministratorAccess"]


#arn:aws:iam::290488660393:role/github-oidc-provider-aws

===== FILE: ./infrastructure/OIDC/variables.tf =====

variable "branch" {
  type    = string
  default = "main"
}

# three entries required
# GitHub sends a different `sub` claim depending on the trigger context.
# The module builds a trust policy using StringLike under the hood,
# so each entry below matches a specific pattern AWS will receive:

#   :ref:refs/heads/main   ← direct push or non-environment workflow_dispatch
#   :environment:dev       ← workflow_dispatch with environment=dev selected
#   :environment:prod      ← workflow_dispatch with environment=prod selected
#
# All three must be present or the OIDC auth fails for that context.
variable "repository" {
  type = list(string)
}

variable "aws_account_id" {
  type = string
}

variable "region" {
  type = string
}

variable "policies" {
  type = list(string)
}


===== FILE: ./infrastructure/state-bucket/main.tf =====

# Terraform State bucket Bootstrap 
# This folder is applied ONCE before anything else, in its own pipeline step.
# It creates the S3 bucket and DynamoDB table that the main kobo-terraform
# folder uses as its remote backend.
#
# Apply order:
#   1. cd state-bucket && terraform apply   ← this folder
#   2. cd infrastrucuture && terraform init && terraform apply
#
# IMPORTANT: This folder itself uses local state (no backend block).
# Its own state file is small — just 2 resources — and stored locally or
# committed to the repo. Do not try to store this state in the bucket it creates.

# S3 bucket for Terraform state 
resource "aws_s3_bucket" "state" { 
  bucket        = var.tf_state_bucket
  force_destroy = false # Never allow accidental deletion of state

  tags = {
    Name      = var.tf_state_bucket
    Purpose   = "Terraform remote state"
    ManagedBy = "terraform"
  }
}

# Block all public access 
# State contains passwords, keys, and connection strings in plaintext.
# This must never be publicly readable under any circumstances.
resource "aws_s3_bucket_public_access_block" "state" {
  bucket = aws_s3_bucket.state.id

  block_public_acls       = true
  block_public_policy     = true
  ignore_public_acls      = true
  restrict_public_buckets = true
}

# Encryption at rest 
# AES-256 SSE-S3 — no extra cost, protects state if someone gains S3 access.
resource "aws_s3_bucket_server_side_encryption_configuration" "state" {
  bucket = aws_s3_bucket.state.id

  rule {
    apply_server_side_encryption_by_default {
      sse_algorithm = "AES256"
    }
    bucket_key_enabled = true
  }
}

# Versioning 
# Critical. If a terraform apply corrupts state, versioning lets you roll back
# to the last known-good version. Without versioning, a corrupted state file
# means manually rebuilding everything.
resource "aws_s3_bucket_versioning" "state" {
  bucket = aws_s3_bucket.state.id

  versioning_configuration {
    status = "Enabled"
  }
}

# Bucket ownership 
resource "aws_s3_bucket_ownership_controls" "state" {
  bucket = aws_s3_bucket.state.id

  rule {
    object_ownership = "BucketOwnerEnforced"
  }
}

# Enforce HTTPS only 
# Deny any request that does not use TLS. Prevents state being read or written
# over an unencrypted connection.
resource "aws_s3_bucket_policy" "state" {
  bucket = aws_s3_bucket.state.id

  # Must wait for public access block to be applied first
  depends_on = [aws_s3_bucket_public_access_block.state]

  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [
      {
        Sid       = "DenyNonTLS"
        Effect    = "Deny"
        Principal = "*"
        Action    = "s3:*"
        Resource = [
          "arn:aws:s3:::${var.tf_state_bucket}",
          "arn:aws:s3:::${var.tf_state_bucket}/*"
        ]
        Condition = {
          Bool = {
            "aws:SecureTransport" = "false"
          }
        }
      },
      {
        Sid    = "DenyNonAccountAccess"
        Effect = "Deny"
        Principal = "*"
        Action = "s3:*"
        Resource = [
          "arn:aws:s3:::${var.tf_state_bucket}",
          "arn:aws:s3:::${var.tf_state_bucket}/*"
        ]
        Condition = {
          StringNotEquals = {
            "aws:PrincipalAccount" = var.aws_account_id
          }
        }
      }
    ]
  })
}

# Lifecycle clean up old state versions 
# Keep the last 90 days of state versions. Beyond that, old versions are
# deleted automatically to prevent unbounded storage growth.
# The current (latest) version is NEVER deleted by this rule.
resource "aws_s3_bucket_lifecycle_configuration" "state" {
  bucket = aws_s3_bucket.state.id

  # Must wait for versioning to be enabled first
  depends_on = [aws_s3_bucket_versioning.state]

  rule {
    id     = "expire-old-state-versions"
    status = "Enabled"

    noncurrent_version_expiration {
      noncurrent_days = 90
    }

    # Clean up incomplete multipart uploads
    abort_incomplete_multipart_upload {
      days_after_initiation = 7
    }
  }
}

# DynamoDB table for state locking
# When two engineers (or two CI jobs) run terraform apply at the same time,
# Terraform writes a lock record here before making any changes.
# The second apply reads the lock and waits. This prevents state corruption
# from concurrent applies.
#
# Pay-per-request billing — stores 1-2 records at a time.
# Cost: ~$0.25/month regardless of team size or apply frequency.
resource "aws_dynamodb_table" "terraform_locks" {
  name         = var.tf_lock_table
  billing_mode = "PAY_PER_REQUEST"
  hash_key     = "LockID"

  attribute {
    name = "LockID"
    type = "S"
  }

  # Protect the lock table from accidental deletion
  deletion_protection_enabled = true

  # Encrypt lock records at rest
  server_side_encryption {
    enabled = true
  }

  tags = {
    Name      = var.tf_lock_table
    Purpose   = "Terraform state locking"
    ManagedBy = "terraform"
  }
}


===== FILE: ./infrastructure/state-bucket/outputs.tf =====

output "state_bucket_name" {
  description = "S3 bucket name used for backend.tf in the main infra"
  value       = aws_s3_bucket.state.id
}

/* output "state_bucket_arn" {
  description = "S3 bucket ARN"
  value       = aws_s3_bucket.state.arn
} */

/* output "lock_table_name" {
  description = "DynamoDB table name — copy this into the main infrastracture backend.tf"
  value       = aws_dynamodb_table.terraform_locks.name
} */

/* output "backend_config" {
  description = "Paste this block into infrastracture/backend.tf after running this bootstrap"
  value       = <<-EOT

    terraform {
      backend "s3" {
        bucket         = "${aws_s3_bucket.state.id}"
        key            = "key name"
        region         = "${var.region}"
        dynamodb_table = "${aws_dynamodb_table.terraform_locks.name}"
        encrypt        = true
      }
    }
  EOT
} */


===== FILE: ./infrastructure/state-bucket/providers.tf =====

terraform {
  # No backend block here this folder uses local state intentionally.
  # Storing bootstrap state in the bucket it creates would be circular.
  # The state file for this folder is tiny (2 resources) and safe to keep locally.

  required_version = ">= 1.11.1"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 6.34"
    }
  }
}

provider "aws" {
  region              = var.region
  allowed_account_ids = [var.aws_account_id]
}


===== FILE: ./infrastructure/state-bucket/.terraform.lock.hcl =====

# This file is maintained automatically by "terraform init".
# Manual edits may be lost in future updates.

provider "registry.terraform.io/hashicorp/aws" {
  version     = "6.51.0"
  constraints = ">= 6.34.0"
  hashes = [
    "h1:bclp+xS1fYeOCil0XZO6mKvEeHFESt5K/XotVSZND54=",
    "zh:03fcea0a1ea2ca81d62d4d2e2961181bef9068b1c701f2cddc4aa5fac105818a",
    "zh:1213944cd623143974ea5c9b70b22ae1ccca33d743924c149ed089d34b8e08b4",
    "zh:190a46da0c69082b74da48238ce134d2fc9893e09122ac249c5689f88eab7e13",
    "zh:1b312a4b53fa3cf731f95e674c033865feea5455f163b86136f2614424637293",
    "zh:2b319814806222c5aba196b1a78756a6b36dc5c91f85edda349234d8a2f20a6a",
    "zh:2bddf92c8efc6ad445a2eb8a0e5f88742a0596392c3a4ebc350ebb4105a4a96d",
    "zh:3bef0c4f675c09034ff017cf899977b1765b2c0b3d1e489bcb06a5fcac316e2d",
    "zh:47c46b5aa22199638fed5c93b195bbfd1182a1408edad4e5c39d4a73a04493f6",
    "zh:5f808699650f6db961964466c77f5a581eab142a91c2e54810bb09b6f2fcd3f2",
    "zh:9b12af85486a96aedd8d7984b0ff811a4b42e3d88dad1a3fb4c0b580d04fa425",
    "zh:ada97e6be10164f452e278c23412b8597698a9c95ffb68fe83629d63d85906f3",
    "zh:c4d73a91810d8dbcf9abbd431d41fcceebb48f8b6fd3c28a84bb3c6ed08be2e9",
    "zh:c63ec875d38fc557b16b0b2b0ab1c7635852799453113240e21a52409de94a71",
    "zh:cdd0209a755fc3aa14855aa013dae4b166a2fc7f6d3cbb673f7ff2142f5b63a2",
    "zh:e5e665a27290391fd1bffc093ab68b596f6c507785be2e3f0949fab4fd6aec1b",
    "zh:f6c42046a31d65eff2793737656b38931f90318b53661046bb84326cd4cb558f",
  ]
}


===== FILE: ./infrastructure/state-bucket/terraform.tfvars =====

aws_account_id  = "290488660393"
region          = "us-east-1"
tf_state_bucket = "terraform-state-290488660393"
tf_lock_table   = "terraform-state-lock"


===== FILE: ./infrastructure/state-bucket/variables.tf =====

variable "aws_account_id" {
  type        = string
  description = "AWS account ID used to restrict bucket access to this account only"
}

variable "region" {
  type        = string
  description = "AWS region where the state bucket and lock table will be created"
}

variable "tf_state_bucket" {
  type        = string
  description = "S3 bucket name for Terraform remote state. Must be globally unique."
}

variable "tf_lock_table" {
  type        = string
  description = "DynamoDB table name for Terraform state locking"
}


