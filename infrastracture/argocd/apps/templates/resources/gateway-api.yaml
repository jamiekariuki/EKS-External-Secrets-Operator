apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Values.gatewayCr.name }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.gatewayCr.syncWaves | quote }}
    argocd.argoproj.io/hook: {{ .Values.gatewayCr.hook }}
spec:
  project: default
  source:
    repoURL: {{ .Values.repoURL }}
    path: {{ .Values.gatewayCr.path }}

  destination:
    server: https://kubernetes.default.svc
    namespace: {{ .Values.gatewayCr.namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - Validate=false
    - ServerSideApply=true
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m