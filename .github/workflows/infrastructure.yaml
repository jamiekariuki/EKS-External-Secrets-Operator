# ── Infrastructure Pipeline ────────────────────────────────────────────────────
#
# Full lifecycle management for EKS/VPC infrastructure + ArgoCD app-of-apps.
# This is a MANUAL workflow — triggered via workflow_dispatch only.
#
# ── Job flow (apply) ──────────────────────────────────────────────────────────
#
#   guard → infra-plan → infra-apply → update-helm → argocd-plan → argocd-apply
#                    ↘ (destroy) → destroy
#
# ── Secrets & config split ────────────────────────────────────────────────────
#
#   terraform.<env>.tfvars  (committed) — region, project, cluster_name, etc.
#   ci-secrets.tfvars       (runtime)   — aws_account_id only; never committed
#
# ── GitHub Secrets required ───────────────────────────────────────────────────
#
#   Repository secrets:
#     AWS_OIDC_ROLE_ARN     — OIDC role with Terraform permissions
#     AWS_REGION
#     TF_STATE_BUCKET
#     TF_LOCK_TABLE
#     SERVICE_ACCOUNT_NAME  — ESO service account name
#
#   Environment secrets (dev / stage / prod):
#     AWS_ACCOUNT_ID
#
# ── Helm charts ───────────────────────────────────────────────────────────────
# Helm charts live in ./charts in this same repo.
# The update-helm job commits changes directly — no PAT or second checkout needed.
#
# ─────────────────────────────────────────────────────────────────────────────

name: "Infrastructure"

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        type: choice
        options:
          - dev
          - stage
          - prod
      action:
        description: "Terraform action"
        required: true
        type: choice
        options:
          - plan
          - apply
          - destroy
        default: plan
      confirm_destroy:
        description: "Type DESTROY to confirm (destroy only)"
        required: false
        type: string
        default: ""

permissions:
  id-token: write
  contents: write  # needed to commit helm chart updates back to this repo

env:
  TF_VERSION: "1.11.1"
  TF_IN_AUTOMATION: "true"
  TF_INPUT: "false"
  INFRA_DIR: "./infrastructure/infra"
  ARGOCD_DIR: "./infrastructure/argocd"

# ── Jobs ───────────────────────────────────────────────────────────────────────

jobs:

  # ── Guard ──────────────────────────────────────────────────────────────────
  guard:
    name: "Guard"
    runs-on: ubuntu-latest
    steps:
      - name: Validate destroy confirmation
        if: github.event.inputs.action == 'destroy'
        run: |
          if [ "${{ github.event.inputs.confirm_destroy }}" != "DESTROY" ]; then
            echo "::error::Destroy requires typing DESTROY in the confirmation field."
            echo "You typed: '${{ github.event.inputs.confirm_destroy }}'"
            exit 1
          fi
          echo "Destroy confirmed."

      - name: Run summary
        run: |
          echo "## Infrastructure Run" >> $GITHUB_STEP_SUMMARY
          echo "| | |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| **Environment** | \`${{ github.event.inputs.environment }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Action** | \`${{ github.event.inputs.action }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Triggered by** | ${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Run ID** | ${{ github.run_id }} |" >> $GITHUB_STEP_SUMMARY

  # ── Infra Plan ─────────────────────────────────────────────────────────────
  infra-plan:
    name: "Infra Plan — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: guard
    environment: ${{ github.event.inputs.environment }}

    outputs:
      plan_exit_code: ${{ steps.plan.outputs.exit_code }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: infra-plan-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Generate ci-secrets.tfvars
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          cat > ci-secrets.tfvars << EOF
          aws_account_id = "${{ secrets.AWS_ACCOUNT_ID }}"
          EOF

      - name: Terraform Init
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=infra/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: Select workspace
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          terraform workspace select ${{ github.event.inputs.environment }} \
            || terraform workspace new ${{ github.event.inputs.environment }}

      - name: Terraform Validate
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform validate

      - name: Terraform Plan
        id: plan
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          ENV="${{ github.event.inputs.environment }}"
          DESTROY_FLAG=""
          [[ "${{ github.event.inputs.action }}" == "destroy" ]] && DESTROY_FLAG="-destroy"

          terraform plan \
            $DESTROY_FLAG \
            -var-file="terraform.${ENV}.tfvars" \
            -var-file="ci-secrets.tfvars" \
            -out=tfplan \
            -detailed-exitcode \
            -no-color 2>&1 | tee plan_output.txt

          # Exit codes: 0 = no changes, 1 = error, 2 = changes pending
          EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          [ "$EXIT_CODE" -eq 1 ] && exit 1
          echo "Plan complete (exit code $EXIT_CODE)."

      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: infra-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: |
            ${{ env.INFRA_DIR }}/tfplan
            ${{ env.INFRA_DIR }}/plan_output.txt
          retention-days: 1

      - name: Plan summary
        run: |
          EXIT_CODE="${{ steps.plan.outputs.exit_code }}"
          echo "### Infra Plan — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          [ "$EXIT_CODE" == "0" ] && echo " No infrastructure changes." >> $GITHUB_STEP_SUMMARY
          [ "$EXIT_CODE" == "2" ] && echo " Changes pending — review before applying." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "<details><summary>Plan output</summary>" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat ${{ env.INFRA_DIR }}/plan_output.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY

  # ── Infra Apply ────────────────────────────────────────────────────────────
  infra-apply:
    name: "Infra Apply — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: infra-plan
    environment: ${{ github.event.inputs.environment }}
    if: |
      github.event.inputs.action == 'apply' &&
      needs.infra-plan.outputs.plan_exit_code == '2'

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: infra-apply-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=infra/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: Select workspace
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform workspace select ${{ github.event.inputs.environment }}

      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: infra-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: ${{ env.INFRA_DIR }}

      - name: Terraform Apply
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform apply -auto-approve tfplan 2>&1 | tee apply_output.txt

      - name: Apply summary
        if: success()
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          echo "### Infra Apply Complete — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          terraform output -no-color 2>/dev/null || echo "No outputs."
          echo '```' >> $GITHUB_STEP_SUMMARY

  # ── Update Helm ────────────────────────────────────────────────────────────
  # Helm charts live in ./charts in this same repo.
  # Outputs are pulled directly from S3 state — no artifact passing needed.
  update-helm:
    name: "Update Helm — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: infra-apply

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ github.token }}  # needs contents: write to push

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: helm-update-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Extract Terraform outputs
        run: |
          chmod +x .github/scripts/extract-tf-outputs.sh
          .github/scripts/extract-tf-outputs.sh \
            "${{ secrets.TF_STATE_BUCKET }}" \
            "${{ github.event.inputs.environment }}" \
            "infra/terraform.tfstate"

      - name: Update Helm chart values
        run: |
          chmod +x .github/scripts/update-helm.sh
          .github/scripts/update-helm.sh \
            "${{ github.event.inputs.environment }}" \
            "${{ secrets.AWS_REGION }}" \
            "${{ env.IRSA_ARN }}" \
            "${{ env.FRONTEND_REPO_URL }}" \
            "${{ env.BACKEND_REPO_URL }}" \
            "./charts"

  # ── ArgoCD Plan ────────────────────────────────────────────────────────────
  argocd-plan:
    name: "ArgoCD Plan — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: update-helm
    environment: ${{ github.event.inputs.environment }}

    outputs:
      plan_exit_code: ${{ steps.plan.outputs.exit_code }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: argocd-plan-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Generate ci-secrets.tfvars
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          cat > ci-secrets.tfvars << EOF
          aws_account_id = "${{ secrets.AWS_ACCOUNT_ID }}"
          EOF

      - name: Terraform Init
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=argocd/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: Select workspace
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          terraform workspace select ${{ github.event.inputs.environment }} \
            || terraform workspace new ${{ github.event.inputs.environment }}

      - name: Terraform Validate
        working-directory: ${{ env.ARGOCD_DIR }}
        run: terraform validate

      - name: Terraform Plan
        id: plan
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          ENV="${{ github.event.inputs.environment }}"

          terraform plan \
            -var-file="terraform.${ENV}.tfvars" \
            -var-file="ci-secrets.tfvars" \
            -out=tfplan \
            -detailed-exitcode \
            -no-color 2>&1 | tee plan_output.txt

          EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=$EXIT_CODE" >> "$GITHUB_OUTPUT"
          [ "$EXIT_CODE" -eq 1 ] && exit 1
          echo "Plan complete (exit code $EXIT_CODE)."

      - name: Upload plan artifact
        uses: actions/upload-artifact@v4
        with:
          name: argocd-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: |
            ${{ env.ARGOCD_DIR }}/tfplan
            ${{ env.ARGOCD_DIR }}/plan_output.txt
          retention-days: 1

      - name: Plan summary
        run: |
          EXIT_CODE="${{ steps.plan.outputs.exit_code }}"
          echo "### ArgoCD Plan — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          [ "$EXIT_CODE" == "0" ] && echo " No ArgoCD changes." >> $GITHUB_STEP_SUMMARY
          [ "$EXIT_CODE" == "2" ] && echo " Changes pending." >> $GITHUB_STEP_SUMMARY
          echo "<details><summary>Plan output</summary>" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat ${{ env.ARGOCD_DIR }}/plan_output.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "</details>" >> $GITHUB_STEP_SUMMARY

  # ── ArgoCD Apply ───────────────────────────────────────────────────────────
  argocd-apply:
    name: "ArgoCD Apply — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: argocd-plan
    environment: ${{ github.event.inputs.environment }}
    if: needs.argocd-plan.outputs.plan_exit_code == '2'

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: argocd-apply-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=argocd/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: Select workspace
        working-directory: ${{ env.ARGOCD_DIR }}
        run: terraform workspace select ${{ github.event.inputs.environment }}

      - name: Download plan artifact
        uses: actions/download-artifact@v4
        with:
          name: argocd-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: ${{ env.ARGOCD_DIR }}

      - name: Terraform Apply
        working-directory: ${{ env.ARGOCD_DIR }}
        run: terraform apply -auto-approve tfplan 2>&1 | tee apply_output.txt

      - name: Apply summary
        if: success()
        run: |
          echo "### ArgoCD Apply Complete — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY

  # ── Destroy ────────────────────────────────────────────────────────────────
  # Order matters: ArgoCD (helm_release app-of-apps) is torn down FIRST so
  # ArgoCD stops reconciling child apps before the EKS cluster is deleted.
  # Destroying infra while ArgoCD is still managing apps causes stuck finalizers.
  #
  # Phase 1 — ArgoCD: terraform destroy (direct, no pre-planned artifact)
  # Phase 2 — Infra:  terraform apply the pre-planned destroy tfplan from infra-plan
  destroy:
    name: "Destroy — ${{ github.event.inputs.environment }}"
    runs-on: ubuntu-latest
    needs: infra-plan
    environment: ${{ github.event.inputs.environment }}
    if: |
      github.event.inputs.action == 'destroy' &&
      github.event.inputs.confirm_destroy == 'DESTROY' &&
      needs.infra-plan.outputs.plan_exit_code == '2'

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: destroy-${{ github.event.inputs.environment }}-${{ github.run_id }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
          terraform_wrapper: false

      # ── Phase 1: Destroy ArgoCD first ──────────────────────────────────────
      # Removes the helm_release (app-of-apps) and lets ArgoCD clean up child
      # apps before the cluster disappears underneath them.

      - name: "[ArgoCD] Generate ci-secrets.tfvars"
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          cat > ci-secrets.tfvars << EOF
          aws_account_id = "${{ secrets.AWS_ACCOUNT_ID }}"
          EOF

      - name: "[ArgoCD] Terraform Init"
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=argocd/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: "[ArgoCD] Select workspace"
        working-directory: ${{ env.ARGOCD_DIR }}
        run: terraform workspace select ${{ github.event.inputs.environment }}

      - name: "[ArgoCD] Terraform Destroy"
        working-directory: ${{ env.ARGOCD_DIR }}
        run: |
          ENV="${{ github.event.inputs.environment }}"
          terraform destroy -auto-approve \
            -var-file="terraform.${ENV}.tfvars" \
            -var-file="ci-secrets.tfvars" \
            2>&1 | tee argocd_destroy_output.txt

      # ── Phase 2: Destroy Infra ─────────────────────────────────────────────
      # Uses the pre-planned destroy tfplan from the infra-plan job.
      # EKS, VPC, RDS, ECR, IAM — everything.

      - name: "[Infra] Generate ci-secrets.tfvars"
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          cat > ci-secrets.tfvars << EOF
          aws_account_id = "${{ secrets.AWS_ACCOUNT_ID }}"
          EOF

      - name: "[Infra] Terraform Init"
        working-directory: ${{ env.INFRA_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ secrets.TF_STATE_BUCKET }}" \
            -backend-config="key=infra/terraform.tfstate" \
            -backend-config="region=${{ secrets.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ secrets.TF_LOCK_TABLE }}" \
            -backend-config="encrypt=true"

      - name: "[Infra] Select workspace"
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform workspace select ${{ github.event.inputs.environment }}

      - name: "[Infra] Download destroy plan artifact"
        uses: actions/download-artifact@v4
        with:
          name: infra-tfplan-${{ github.event.inputs.environment }}-${{ github.run_id }}
          path: ${{ env.INFRA_DIR }}

      - name: "[Infra] Terraform Destroy"
        working-directory: ${{ env.INFRA_DIR }}
        run: terraform apply -auto-approve tfplan 2>&1 | tee infra_destroy_output.txt

      - name: Destroy summary
        if: always()
        run: |
          echo "### Destroy Complete — ${{ github.event.inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Phase | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---|---|" >> $GITHUB_STEP_SUMMARY
          echo "| ArgoCD (app-of-apps) | destroyed first ✓ |" >> $GITHUB_STEP_SUMMARY
          echo "| Infrastructure (EKS/VPC/RDS) | destroyed second ✓ |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Triggered by: **${{ github.actor }}**" >> $GITHUB_STEP_SUMMARY